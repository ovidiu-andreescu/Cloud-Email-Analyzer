# Figure Generation Scripts

No external figure-generation scripts are required for the current paper.

The architecture, pipeline, data-model, and ML comparison figures are written directly in TikZ/PGFPlots under `paper/figures/`.

Future scripts should be added here only when a figure becomes too complex to maintain directly in LaTeX.
