\begin{tikzpicture}[
  font=\footnotesize,
  entity/.style={draw, rounded corners=2pt, align=left, text width=3.55cm, minimum height=1.05cm, fill=white},
  artifact/.style={draw, rounded corners=2pt, align=left, text width=3.65cm, minimum height=0.92cm, fill=white},
  arrow/.style={-{Latex[length=2mm]}, thick},
  biarrow/.style={<->, >=Latex, thick},
  node distance=1.0cm and 1.15cm
]
\node[entity] (messages) {\textbf{Message ledger}\\messageId\\status, verdicts\\artifact pointers};
\node[entity, left=of messages] (mailboxes) {\textbf{Mailboxes}\\emailAddress\\ownerUserIds\\mailboxType};
\node[entity, above=of mailboxes] (users) {\textbf{Users}\\userId\\email, role\\password metadata};
\node[entity, right=of messages, text width=4.35cm] (attachments) {\textbf{Attachments}\\\mbox{messageId + attachmentId}\\\mbox{sha256, scan verdict}\\object pointer};
\node[entity, below=of messages] (inbox) {\textbf{Inbox projection}\\userId + sort key\\message summary\\final verdict};
\node[entity, below=of attachments] (audit) {\textbf{Audit records}\\tenantId + sort key\\actor, action\\metadata};

\node[artifact, above=of messages] (raw) {\textbf{Raw MIME}\\inbound message object};
\node[artifact, above=of attachments] (parsed) {\textbf{Derived artifacts}\\parsed body\\\mbox{extracted attachments}};

\draw[biarrow] (users) -- (mailboxes);
\draw[arrow] (mailboxes.south) |- (inbox.west);
\draw[arrow] (raw) -- (messages);
\draw[arrow] (raw) -- (parsed);
\draw[arrow] (parsed) -- (attachments);
\draw[arrow] (messages) -- (attachments);
\draw[arrow] (messages) -- (inbox);
\draw[arrow] (messages) -- (audit);
\draw[arrow] (attachments) -- (audit);
\end{tikzpicture}
