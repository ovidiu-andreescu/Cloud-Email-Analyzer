# Cloud-Email-Analyzer
Cloud-native email attachment security analyzer. Ingests emails, scans attachments in isolated containers or VMs, applies static/dynamic analysis, extracts IoCs with AI, and generates verdicts &amp; reports. Suspicious emails are quarantined with safe notifications.
