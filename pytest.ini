[pytest]
testpaths = tests
addopts =
    -v
    -ra
    --maxfail=1
    --strict-markers
markers =
    integration: integration deps
    unit: unit tests
asyncio_mode = auto
xfail_strict = true
python_files = test_*.py
python_classes = Test*
python_functions = test_*